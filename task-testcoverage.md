This project is having a large refactoring. We need to have unit tests to have
more test coverage.

1. Measure the current test coverage.
2. Identify if we can add a new test to increase test coverage.
3. Add such test at ./src/tests/ with a new file, so that `cargo test` will
   catch up.

