[package]
name = "hmux-agent"
version = "0.1.0"
edition = "2024"
description = "Agent detection and pane observability contracts shared by tmux-compatible servers"
license = "GPL-3.0-only"

[lints]
workspace = true

[dependencies]
libc = "0.2"
tracing = "0.1"

[dev-dependencies]
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
